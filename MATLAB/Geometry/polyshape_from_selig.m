function [p] = polyshape_from_selig(filename)
%POLYSHAPE_FROM_SELIG Costruisci una polyshape a partire da un file
% che descrive un profilo alare in formato Selig.
    vxy = readmatrix(filename);
end

